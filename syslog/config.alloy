livedebugging {
  enabled = true
}

loki.source.syslog "local" {
  listener {
    address  = "0.0.0.0:51893"
    labels   = { component = "loki.source.syslog", protocol = "tcp" }
  }

  listener {
    address  = "0.0.0.0:51898"
    protocol = "udp"
    labels   = { component = "loki.source.syslog", protocol = "udp"}
  }

  forward_to = [loki.write.local.receiver]
  relabel_rules = discovery.relabel.syslog.rules
}

loki.write "local" {
  endpoint {
    url = "http://loki:3100/loki/api/v1/push"
  }
}

discovery.relabel "syslog" {
    targets = []
    
    # Mapear hostname para label host
    rule {
        source_labels = ["__syslog_message_hostname"]
        target_label = "host"
    }
    
    # Mapear application name
    rule {
        source_labels = ["__syslog_message_app_name"]
        target_label = "application"
    }
    
    # Identificar tipo de dispositivo baseado no hostname/IP
    rule {
        source_labels = ["__syslog_message_hostname"]
        regex = ".*olt.*|.*OLT.*"
        target_label = "device_type"
        replacement = "olt"
    }
    
    rule {
        source_labels = ["__syslog_message_hostname"]
        regex = ".*switch.*|.*datacom.*|.*SWITCH.*"
        target_label = "device_type"
        replacement = "switch"
    }
    
    rule {
        source_labels = ["__syslog_message_hostname"]
        regex = ".*mikrotik.*|.*routeros.*|.*MIKROTIK.*"
        target_label = "device_type"
        replacement = "mikrotik"
    }
    
    # Mapear facility e severity
    rule {
        source_labels = ["__syslog_message_facility"]
        target_label = "facility"
    }
    
    rule {
        source_labels = ["__syslog_message_severity"]
        target_label = "severity"
    }
}

